[base]
swarm_type = 'aggregation'
n_agents = 50
object_location = [500, 500]

# two same size sites
# left = [250, 500]
# right = [750, 500]
# left_size = [100, 100]
# right_size = [100, 100]

# two different size sites
# left = [250, 500]
# right = [750, 500]
# left_size = [100, 100]
# right_size = [125, 125]

# two different size sites bonus
left = [500, 500]
right = [500, 250]
left_size = [100, 100]
right_size = [125, 125]

[screen]
frames = inf
height = 1000
width = 1000

[aggregation]
sites = true
one = false

[cockroach]
#velocity force
max_force = 8.0
radius_view = 50
p_change = 0.01
#joiningprobs
p_join_base = 0.4
join_weight = 0.25
p_join_max = 0.99
t_stop = 50
t_stop_var = 25
#leavingprobs
critical_mass = 12
p_leave_base = 0.006
leave_weight = 0.001
p_leave_min = 0.0001
t_leave = 100
t_leave_var = 50

[agent]
height = 8
width = 10
dt = 0.2
mass = 20
max_speed = 7.0
min_speed = 4.0

# Specification: https://toml.io/
